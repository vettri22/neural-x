<<<<<<< HEAD
# NEURAL-X v2 Environment Variables
# Copy to .env and fill in real values.

# ── Flask ────────────────────────────────────────────────────────────────────
SECRET_KEY=change-me-to-a-random-64-char-string
FLASK_ENV=development        # development | production | testing

# ── Database ─────────────────────────────────────────────────────────────────
# Leave blank to use default SQLite file
DATABASE_URL=

# ── External Threat Intelligence APIs (all optional) ─────────────────────────
GOOGLE_SAFE_BROWSING_API_KEY=
VIRUSTOTAL_API_KEY=
ABUSEIPDB_API_KEY=

# ── v2: Journal Verifier settings ────────────────────────────────────────────
# Set to false to disable live external calls (useful in air-gapped environments)
JOURNAL_DOAJ_ENABLED=true
JOURNAL_CROSSREF_ENABLED=true
JOURNAL_OPENALEX_ENABLED=true
JOURNAL_CONTENT_SCAN=true
JOURNAL_REQUEST_TIMEOUT=10

# ── v3: Website Classifier & parallel workers ─────────────────────────────────
JOURNAL_ROR_ENABLED=true
JOURNAL_PARALLEL_WORKERS=4
=======
# NEURAL-X Environment Configuration
# Copy this file to .env and fill in your values

# ── Flask Core ──────────────────────────────────────────────
FLASK_ENV=development
SECRET_KEY=change-this-to-a-long-random-secret-key-in-production
PORT=5000

# ── Database ────────────────────────────────────────────────
# SQLite (default — no setup needed):
# DATABASE_URL=sqlite:///instance/neural_x.db

# PostgreSQL (production recommended):
# DATABASE_URL=postgresql://user:password@localhost:5432/neural_x

# ── Security Intelligence APIs ──────────────────────────────
# Google Safe Browsing (free tier available):
# https://developers.google.com/safe-browsing/v4/get-started
GOOGLE_SAFE_BROWSING_API_KEY=your_google_safe_browsing_api_key_here

# VirusTotal (free 4 req/min):
# https://www.virustotal.com/gui/my-apikey
VIRUSTOTAL_API_KEY=your_virustotal_api_key_here

# AbuseIPDB (free tier available):
# https://www.abuseipdb.com/account/api
ABUSEIPDB_API_KEY=your_abuseipdb_api_key_here

# ── Screenshot Service ──────────────────────────────────────
SCREENSHOT_TIMEOUT=15
SCREENSHOT_WIDTH=1280
SCREENSHOT_HEIGHT=720

# ── Rate Limiting ───────────────────────────────────────────
RATELIMIT_DEFAULT=100 per hour

# ── Production Settings (set these in production) ───────────
# SESSION_COOKIE_SECURE=true
# WTF_CSRF_ENABLED=true
>>>>>>> 99727748a15251a8f4d92432e4608bc61952b66f
